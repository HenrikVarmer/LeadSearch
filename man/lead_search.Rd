% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lead_search.R
\name{lead_search}
\alias{lead_search}
\title{Enrich a data frame of leads with job, workplace and LinkedIn information}
\usage{
lead_search(x, engine = c("google", "bing"), api_key, cx, delay = 0.2)
}
\arguments{
\item{x}{A data frame (or object coercible to one) whose \strong{first
column is the lead's name} and \strong{second column is the lead's
e-mail}, in that order.}

\item{engine}{The search engine to use: either \code{"google"} (the default)
or \code{"bing"}.}

\item{api_key}{The CustomSearch API key for the chosen engine.}

\item{cx}{The CustomSearch engine / configuration id for the chosen engine.}

\item{delay}{Seconds to pause before each HTTP request, to respect API rate
limits. Defaults to \code{0.2}.}
}
\value{
\code{x} with three additional character columns: \code{title},
\code{workplace} and \code{link}.
}
\description{
For each (name, e-mail) pair in \code{x}, queries the chosen search engine's
CustomSearch API (restricted to LinkedIn) and appends three columns
containing the best-guess job \code{title}, \code{workplace} and LinkedIn
profile \code{link}.
}
\examples{
\dontrun{
leads <- data.frame(
  name = c("Jane Doe", "John Doe"),
  mail = c("jane@acme.com", "john@gmail.com"),
  stringsAsFactors = FALSE
)
lead_search(x = leads, api_key = "YOUR_KEY", cx = "YOUR_CX")
}

}
