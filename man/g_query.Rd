% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g_query.R
\name{g_query}
\alias{g_query}
\title{Query the Google CustomSearch API for a single lead}
\usage{
g_query(api_key, cx, name, mail, delay = 0.2)
}
\arguments{
\item{api_key}{Google CustomSearch API key.}

\item{cx}{Google CustomSearch engine (context) id.}

\item{name}{The lead's full name.}

\item{mail}{The lead's e-mail address.}

\item{delay}{Seconds to pause before each HTTP request (politeness / rate
limiting). Defaults to \code{0.2}.}
}
\value{
A named character vector with elements \code{title},
\code{workplace} and \code{link}.
}
\description{
Performs a Google CustomSearch query restricted to \code{linkedin.com} for a
single (name, e-mail) pair and returns the best-guess job title, workplace
and LinkedIn profile URL.
}
\details{
Each candidate URL is fetched \strong{exactly once}; the parsed response is
reused for all downstream extraction. Non-200 responses, network errors and
empty result sets are handled gracefully and surfaced as \code{"No Result"}
rather than raising an error.
}
